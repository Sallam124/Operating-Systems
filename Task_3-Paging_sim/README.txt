Create a file named paging_text.txt (or any name you prefer) and place the page reference IDs inside, one per line. For example:

Make sure to replace with actual path to your file 

string filename = "path_to_your_file.txt";  // Update this with your actual file path


To Compile on linux terminal: 

  g++ -o compling file file name .cpp
  
  ./compling file.

