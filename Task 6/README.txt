Run the generate files.bat to generate the directory with the test files

Compile the c++ file in terminal using the commands found in Compile_Command line

Ensure Correct path to the directory, then run compiled file

